import React from 'react';

const Profile = () => (
  <div className="profile">
    <h1>Profile</h1>
  </div>
);

// Rec.propTypes = {
//   places: React.PropTypes.object,
// };

export default Profile;
